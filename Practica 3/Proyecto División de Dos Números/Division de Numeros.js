var a = prompt("Ingrese el primer numero")
var b = prompt("Ingrese el segundo numero")

if (b == 0)
{
 document.write("No se puede dividir entre cero 0 ");
} 
else
{
    document.write("El resueltado es: " + a/b);
}
